export const metadata = {
  title: 'Admin',
  description: 'Painel administrativo da Acatalog Tech.',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
