export const metadata = {
  title: 'Catalogo',
  description: 'Catalogo de hardware, componentes e perifericos da Acatalog Tech.',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
