export const metadata = {
  title: 'Produto',
  description: 'Detalhes, fotos, frete e compra de produto na Acatalog Tech.',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
